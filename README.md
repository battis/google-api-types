# @battis/google.calendar

Typescript types to describe the responses from the Google Calendar v3 REST API

These types are bashed together quickly from the [Google Calendar API v3 reference](https://developers.google.com/calendar/api/v3/reference) and are meant mostly as a convenience for working with the responses to the REST API in TypeScript.